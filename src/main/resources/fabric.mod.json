{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${authors}"
  ],
  "contact": {
    "homepage": "${website}",
    "sources": "${source_url}",
    "issues": "${issue_tracker}"
  },
  "license": "${license}",
  "icon": "${icon}",
  "environment": "client",
  "entrypoints": {
    "main": [
      {
        "value": "${fabric_common_entry}",
        "adapter": "kotlin"
      }
    ],
    "modmenu": [
      {
        "value": "dev.timlohrer.spotify_overlay.config.SpotifyOverlayConfigScreen",
        "adapter": "kotlin"
      }
    ]
  },
  ${mixin_field}
  ${dependencies_field}
}